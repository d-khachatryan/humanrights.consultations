@model eLConsultation.Data.OralConsultationOrganizationItem

@Html.HiddenFor(model => model.ID)
@Html.HiddenFor(model => model.OralConsultationOrganizationID)
@Html.HiddenFor(model => model.OralConsultationID)

<div class="card-body">
    <div class="form-group">
       @Html.LabelFor(model => model.OrganizationID, new { @class = "control-label" })
    @(Html.Kendo().ComboBoxFor(model => model.OrganizationID)
                        .DataTextField("Text")
                        .DataValueField("Value")
                        .Placeholder("-- Ընտրել --")
                        .Filter(FilterType.StartsWith)
                        .HtmlAttributes(new { @class = "form-control", data_value_primitive = "true" })
                        .DataSource(source =>
                        {
                            source.Read(read =>
                            {
                                read.Action("OrganizationList", "OralConsultationOrganization");
                            });
                        })
    )
    </div>
</div>
